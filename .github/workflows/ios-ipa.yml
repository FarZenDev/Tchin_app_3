name: iOS IPA

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  build-ios-ipa:
    name: Build unsigned iOS IPA
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Disable Swift Package Manager
        run: flutter config --no-enable-swift-package-manager

      - name: Install dependencies
        run: flutter pub get

      - name: Precache iOS artifacts
        run: flutter precache --ios

      - name: Build iOS app without code signing
        run: flutter build ios --release --no-codesign

      - name: Package unsigned IPA
        run: |
          IPA_DIR="build/ios/ipa"
          LATEST_DIR="outputs/ios/latest"
          PAYLOAD_DIR="$IPA_DIR/Payload"
          APP_PATH="build/ios/iphoneos/Runner.app"
          IPA_PATH="$IPA_DIR/tchin_app_3-unsigned.ipa"
          LATEST_IPA_PATH="$LATEST_DIR/tchin_app_3.ipa"

          test -d "$APP_PATH"
          rm -rf "$IPA_DIR" "$LATEST_DIR"
          mkdir -p "$PAYLOAD_DIR" "$LATEST_DIR"
          cp -R "$APP_PATH" "$PAYLOAD_DIR/"
          (cd "$IPA_DIR" && zip -qry "$(basename "$IPA_PATH")" Payload)
          cp "$IPA_PATH" "$LATEST_IPA_PATH"
          ls -lh "$IPA_PATH" "$LATEST_IPA_PATH"

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: tchin-app-3-ios-latest
          path: outputs/ios/latest/tchin_app_3.ipa
          if-no-files-found: error
